# ============================================================
# FlyRank - Task Management API
# Requirements File - Best Practice (Pinned Versions)
# ============================================================

# --- Core Framework ---
fastapi==0.139.0          # Main web framework
uvicorn[standard]==0.51.0 # ASGI server (includes uvloop, httptools, websockets)

# --- Data Validation ---
pydantic==2.13.4          # Schema validation and serialization
pydantic-settings==2.14.2 # Settings management via environment variables

# --- HTTP Client (for testing and external calls) ---
httpx==0.28.1             # Async-capable HTTP client (used by FastAPI TestClient)

# --- Environment & Configuration ---
python-dotenv==1.2.2      # Load environment variables from .env file

# --- Form & File Handling ---
python-multipart==0.0.32  # Required for form data support in FastAPI

# --- Database ---
sqlalchemy==2.0.30        # Database ORM
